---
Parameters:
  Env:
    Description: Current env
    Type: String
    Default: 'dev'
    
Resources:
  S3Bucket:
    Type: 'AWS::S3::Bucket'
    DeletionPolicy: Retain
    Properties:
      BucketName: !Sub avneet-test-${Env}